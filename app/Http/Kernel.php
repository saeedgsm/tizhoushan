<?php

namespace App\Http;

use App\Http\Middleware\CheckAdminAuthenticated;
use App\Http\Middleware\CheckAgencyAuthenticated;
use App\Http\Middleware\CheckEmailPhoneStudent;
use App\Http\Middleware\CheckOfficeAuthenticated;
use App\Http\Middleware\CheckStudentAuthenticated;
use App\Http\Middleware\CheckStudentInfo;
use App\Http\Middleware\CheckStudentRegistrationFee;
use App\Http\Middleware\CheckStudentTariffBases;
use App\Http\Middleware\CheckTeacherAuthenticated;
use App\Http\Middleware\HistoryBack;
use Illuminate\Foundation\Http\Kernel as HttpKernel;

class Kernel extends HttpKernel
{
    /**
     * The application's global HTTP middleware stack.
     *
     * These middleware are run during every request to your application.
     *
     * @var array
     */
    protected $middleware = [
        // \App\Http\Middleware\TrustHosts::class,
        \App\Http\Middleware\TrustProxies::class,
        \Fruitcake\Cors\HandleCors::class,
        \App\Http\Middleware\CheckForMaintenanceMode::class,
        \Illuminate\Foundation\Http\Middleware\ValidatePostSize::class,
        \App\Http\Middleware\TrimStrings::class,
        \Illuminate\Foundation\Http\Middleware\ConvertEmptyStringsToNull::class,
    ];

    /**
     * The application's route middleware groups.
     *
     * @var array
     */
    protected $middlewareGroups = [
        'web' => [
            \App\Http\Middleware\EncryptCookies::class,
            \Illuminate\Cookie\Middleware\AddQueuedCookiesToResponse::class,
            \Illuminate\Session\Middleware\StartSession::class,
            // \Illuminate\Session\Middleware\AuthenticateSession::class,
            \Illuminate\View\Middleware\ShareErrorsFromSession::class,
            \App\Http\Middleware\VerifyCsrfToken::class,
            \Illuminate\Routing\Middleware\SubstituteBindings::class,
        ],

        'api' => [
            'throttle:60,1',
            \Illuminate\Routing\Middleware\SubstituteBindings::class,
        ],
    ];

    /**
     * The application's route middleware.
     *
     * These middleware may be assigned to groups or used individually.
     *
     * @var array
     */
    protected $routeMiddleware = [
        'role' => \Spatie\Permission\Middlewares\RoleMiddleware::class,
        'permission' => \Spatie\Permission\Middlewares\PermissionMiddleware::class,
        'role_or_permission' => \Spatie\Permission\Middlewares\RoleOrPermissionMiddleware::class,
        'prevent-back-history' => HistoryBack::class,
        'checkAdmin' => CheckAdminAuthenticated::class,

        'checkTeacher' => CheckTeacherAuthenticated::class,

        'checkAgency' => CheckAgencyAuthenticated::class,

        'checkOffice' => CheckOfficeAuthenticated::class,

        'checkStudent' => CheckStudentAuthenticated::class,
        'checkStudentRegistrationFee' => CheckStudentRegistrationFee::class,
        'checkStudentInfo' => CheckStudentInfo::class,
        'checkStudentTariffBases' => CheckStudentTariffBases::class,

        'auth' => \App\Http\Middleware\Authenticate::class,
        'auth.basic' => \Illuminate\Auth\Middleware\AuthenticateWithBasicAuth::class,
        'bindings' => \Illuminate\Routing\Middleware\SubstituteBindings::class,
        'cache.headers' => \Illuminate\Http\Middleware\SetCacheHeaders::class,
        'can' => \Illuminate\Auth\Middleware\Authorize::class,
        'guest' => \App\Http\Middleware\RedirectIfAuthenticated::class,
        'password.confirm' => \Illuminate\Auth\Middleware\RequirePassword::class,
        'signed' => \Illuminate\Routing\Middleware\ValidateSignature::class,
        'throttle' => \Illuminate\Routing\Middleware\ThrottleRequests::class,
        'verified' => \Illuminate\Auth\Middleware\EnsureEmailIsVerified::class,
    ];
}
